import 'package:flutter/material.dart';

abstract final class AppColors {
  static const background = Color(0xFF0F172A);
  static const backgroundDark = Color(0xFF070B19);
  static const surface = Color(0xFF1E293B);
  static const surfaceMuted = Color(0xFF1C2541);
  static const primary = Color(0xFF4F46E5);
  static const primaryAccent = Colors.indigoAccent;
  static const drawerGradientStart = Color(0xFF312E81);
  static const drawerGradientEnd = Color(0xFF1E1B4B);
  static const onPrimary = Colors.white;
  static const onSurface = Colors.white70;
  static const onSurfaceMuted = Colors.white38;
  static const onSurfaceHint = Colors.white30;
  static const border = Colors.white10;
  static const error = Colors.redAccent;
  static const success = Colors.green;
  static const offline = Colors.orangeAccent;
  static const online = Colors.greenAccent;
  static const buttonForeground = Color(0xFF0A1128);
}
